{
  "name": "dfm-for-scientists",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "slidev build",
    "dev": "slidev --open",
    "dev:lan": "slidev --remote",
    "dev:tunnel": "node scripts/ensure-cloudflared.mjs && slidev --remote --tunnel",
    "export": "slidev export",
    "fonts": "node scripts/fetch-fonts.mjs"
  },
  "dependencies": {
    "@slidev/cli": "^52.19.1",
    "@slidev/theme-default": "^0.25.0"
  }
}
